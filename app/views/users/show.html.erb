<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>


<%= render partial: '/books/list', locals: { books: @user.books } %>
<%= render partial: '/books/form', locals: { user: @user, book: @user.books.new } %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

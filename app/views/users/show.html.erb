<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<% if @user.books.any? %>
  <p>
    <strong>Books:</strong>
    <ul>
      <% @user.books.each do |book| %>
        <li>
          <%= book.title %>
          <%= link_to 'Destroy', destroy_book_user_path(@user, book_id: book.id), method: :delete, data: { confirm: 'Are you sure?' } %>
        </li>
      <% end %>
    </ul>
  </p>
<% end %>

<%= render partial: 'book_form', locals: { user: @user } %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
